UI_CAMERA_MODE=mock
# Set live to use the device camera and local photo QR decoding.
UI_API_MODE=mock
UI_WALLET_MODE=mock
# Live preview: UI_API_MODE=live, UI_WALLET_MODE=mock
# Live signing: UI_API_MODE=live, UI_WALLET_MODE=metamask
# UI_API_BASE_URL=https://shomei-kun-integration.dptr.workers.dev
# UI_PUBLIC_URL=https://shomei-kun-integration.dptr.workers.dev/ui/
# UI_SAMPLE_CARD_ID=connectivity-20260926-001

# 実ウォレット利用時のCSP許可先。URLのoriginのみ。
# UI_RPC_ORIGIN=https://your-public-rpc.example
